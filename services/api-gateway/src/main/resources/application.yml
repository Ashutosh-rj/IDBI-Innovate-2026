server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      default-filters:
        - name: RequestRateLimiter
          args:
            redis-rate-limiter.replenishRate: 50
            redis-rate-limiter.burstCapacity: 100
            redis-rate-limiter.requestedTokens: 1
            key-resolver: "#{@userKeyResolver}"
      routes:
        # Route to MSME Registry Service (Udyam & KYC)
        - id: msme-registry-route
          uri: ${ROUTING_MSME_REGISTRY_URL:http://localhost:8081}
          predicates:
            - Path=/api/v1/registry/**
        # Route to Consent & AA Service (ReBIT AA v2.0 Flow)
        - id: consent-aa-route
          uri: ${ROUTING_CONSENT_AA_URL:http://localhost:8082}
          predicates:
            - Path=/api/v1/consent/**
        # Route to Ingestion Service (Alt Data Streams)
        - id: ingestion-route
          uri: ${ROUTING_INGESTION_URL:http://localhost:8083}
          predicates:
            - Path=/api/v1/ingestion/**
        # Route to Health Card Service (Immutable Scorecards & OCEN)
        - id: health-card-route
          uri: ${ROUTING_HEALTH_CARD_URL:http://localhost:8084}
          predicates:
            - Path=/api/v1/health-card/**
        # Route to Python FastAPI Scoring Engine (/score & /simulate) - Stricter rate limits for ML inference
        - id: scoring-engine-route
          uri: ${ROUTING_SCORING_ENGINE_URL:http://localhost:8000}
          predicates:
            - Path=/api/v1/score/**, /api/v1/simulate/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                redis-rate-limiter.requestedTokens: 1
                key-resolver: "#{@userKeyResolver}"

jwt:
  secret: ${JWT_SECRET:idbi_innovate_2026_super_secret_jwt_signing_key_at_least_256_bits}
  expiration-ms: 86400000

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway,prometheus
  endpoint:
    health:
      show-details: always
